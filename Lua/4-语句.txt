1.赋值，lua里的多重赋值，a,b=10,20 ，赋值后a为10，b为20，需要知道的是，多重赋值时lua先对等号右边的所有元素求值，然后才执行赋值，所以，x,y=y,x可以用来交换x和y的值。
而且，lua会自动将右边值的个数调整到与左边的变量个数一致。如果值个数较少，那么多余的变量会被赋为nil；如果值多了，就会被丢弃掉。

2.局部变量与块(block)
lua通过local语句创建局部变量